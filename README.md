# dmr-packet-manager
Tiny packet manager 

## Dependencies:
curl, git

## Installation

Run the following command to install:

```bash
git clone https://github.com/mavexe/dmr-packet-manager.sh | bash
